
<% if (users.length > 0) { %>
    <h1>There are <%- users.length %> freelancers from you to choose from</h1>
<% } %>

<div class="list-group">
    <% for(let i = 0; i < users.length; i++) { %>
        <a href="#" class="list-group-item list-group-item-action flex-column align-items-start">
            <div class="d-flex w-100 justify-content-between">
            <h5 class="mb-1"><%- users[i].name%> 
                <span class="badge badge-success">
                <% if (users[i].legalSearchKnowHow) { %> 
                  Online Legal Research
                <% } else { %> 
                  nothing
                <% } %>
                </span></h5>
            <small>3 days ago</small>
            </div>
            <p class="mb-1">Donec id elit non mi porta gravida at eget metus. Maecenas sed diam eget risus varius blandit.</p>
            <small><%- users[i].skills%></small>
        </a>
    <% } %>
</div>



<div class="row mt-5">
    <div class="col-md-6 m-auto">
        <div class="card card-body text-center">
            <h1>LEX PMC Freelancers</h1>
            <p>Create an account or login</p>
            <h1><i class="far fa-user-circle fa-1x"></i></h1>
            <a href="/users/register" class="btn btn-primary btn-block mb-3 registerGreen">Register</a>
            <a href="/users/login" class="btn btn-secondary btn-block loginBlue">Login</a>
        </div>
    </div>
</div>